# Headless CLI only -- no PyQt6, no GUI, no X11.
# imageio / imageio-ffmpeg were dropped with processing/ovf_video.py.
numpy>=1.23.0
scipy>=1.10.0
pandas>=1.5.0
matplotlib>=3.7.0

# Optional: only needed for `cli.py modes --pptx`
python-pptx>=0.6.21
pillow>=9.0.0
